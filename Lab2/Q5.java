package Lab2;
/*
class FindMax {
    
    public static <E extends Comparable<E>> E max(E[] list) {
        E max = list[0];

        for (int i = 1; i < list.length; i++) {
            if (max.compareTo(list[i]) < 0) {
                max = list[i];
            }
        }
        return max;
    }

    class Circle<E> {
        private E[] radius;

        public Circle(E[] radius) {
            this.radius = radius;
        }

        public E[] getRadius() {
            return radius;
        }
    }
}

public class Q5 {
    public static void main(String[] args) {
        
        FindMax findMax = new FindMax();
        
        Integer [] a = {1, 2, 3};
        String [] b = {"red","green","blue"};
        Double [] c = {3.0,2.9,5.9};
        
        FindMax.Circle<Integer> circle = findMax.new Circle<>(a);

        System.out.print("\nReturn value of array a: " + FindMax.max(a));
        System.out.print("\nReturn value of array b: " + FindMax.max(b));
        System.out.print("\nReturn value of array c: " + FindMax.max(c));
        
    }
}
*/